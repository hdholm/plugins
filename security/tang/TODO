Make the iisten interface less manual.  Use an interface dropdown and for each interface add appropriate
   IPv4 and IPv6 forwarders to a localhost listener.  Consider modifying the Tang server to listen on multiple
   IPs.
Add log output page
make jwdir configurable
  - Add to form config for service
  - Use config value in rekey service action
